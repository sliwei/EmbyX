{
  "name": "embyx",
  "version": "1.0.0",
  "description": "Emby 短视频播放器",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev --prefix web\" \"node server/index.js\"",
    "build": "npm run build --prefix web && npm ci --prefix server --omit=dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
