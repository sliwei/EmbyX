---
description: EmbyX 仓库结构与协作约定（全局）
alwaysApply: true
---

# EmbyX 项目上下文

- **定位**：面向 Emby / Jellyfin 的 Web 原生短视频式播放器；数据本地、PWA、键鼠与 TV 遥控适配。
- **目录**：本地开发根目录 `npm run dev` 并行跑 `web/`（Vite）与 `server/`（Node）。生产 Docker：根 `Dockerfile` 多阶段构建 `web/dist` → `web-public`，Node 同源托管 SPA（`SERVE_STATIC=1`）。可选前后端分离：`SERVE_STATIC=0` + 自行用 Nginx 托管 `web/dist`（参考 `web/nginx.conf`）。CI 见 `.github/workflows`。
- **依赖**：`web/`、`server/` 为独立 npm 工程；根 `package.json` 仅编排脚本（如 `concurrently`）。勿随意加无关依赖。
- **修改原则**：改动聚焦需求；避免无关重构；播放、鉴权与 Emby API 相关变更需保守验证。
